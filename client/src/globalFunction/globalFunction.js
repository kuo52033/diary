// import decode from "jwt-decode";

// export const checkAccountExp = (user) => {
//   if (user) {
//     const decodeToken = decode(user?.token);
//     if (decodeToken.exp * 1000 < new Date().getTime()) return true;
//   }
//   return false;
// };
